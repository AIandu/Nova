kind = "web"
previewPath = "/"
title = "Nova — AI&U Platform"
version = "1.0.0"
id = "artifacts/nova"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18131

[services.development]
run = "pnpm --filter @workspace/nova run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nova", "run", "build" ]
serve = "static"
publicDir = "artifacts/nova/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18131"
BASE_PATH = "/"
